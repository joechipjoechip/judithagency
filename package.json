{
	"name": "judithtestroot",
	"private": false,
	"version": "0.1.0",
	"description": "back and front",
	"scripts": {
		"build-backend": "cd backend && npm install && npm run build",
		"build-frontend": "cd frontend && npm install && npm run generate",
		"start-backend": "cd backend && npm run start",
		"start": "node index.js",
	},
	"author": {
		"name": "Lionel O"
	},
	"engines": {
		"node": "14.18.0",
		"npm": ">=6.0.0"
	},
	"license": "MIT",
	"dependencies": {
		"express": "^4.17.3"
	}
}
